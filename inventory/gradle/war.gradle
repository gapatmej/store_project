apply plugin: "war"

bootWar {
    mainClassName = "com.unir.inventory.InventoryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
